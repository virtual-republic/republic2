# The ledger only ever grows. When two branches each append, both sides are
# genuine records — so keep every line from both rather than writing conflict
# markers into a file that must parse as JSON. `republic doctor --repair` then
# re-chains them in time order.
ledger/events.jsonl merge=union
ballots/**/*.json   merge=union
acts/*.json         merge=union
